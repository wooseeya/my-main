# main.py 실행에 필요한 파이썬 패키지 목록.
# integrations/ 폴더(registry.py, molit.py, building_ledger.py, geocode.py,
# land_ledger.py, sens.py)와 config.py까지 직접 확인해서 반영했어요.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0
anthropic>=0.34
openpyxl>=3.1
httpx>=0.27
# 엑셀/이미지 업로드(File, UploadFile)를 쓰는 요청을 FastAPI가 처리하려면 필요
python-multipart>=0.0.9

# --- integrations/registry.py(등기부등본 PDF 분석) 전용 ---
# 이 세 개는 registry.py 안에서 "설치 안 돼 있으면 None으로 두고 안내 메시지만
# 낸다"는 방식으로 감싸놨어서, 없어도 서버 자체는 정상적으로 켜져요. 다만 없으면
# 등기부등본 자동분석 기능만 동작을 안 해요.
pymupdf>=1.24        # PDF에서 텍스트 추출 (import pymupdf, 구버전 호환용 fitz)
Pillow>=10.0          # pytesseract가 이미지를 다루기 위해 필요
pytesseract>=0.3.10   # OCR(스캔본 등기부등본을 텍스트로 인식)
